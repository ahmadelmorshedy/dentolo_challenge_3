class ToyRobot
  def initialize(table_top = TableTop.new)
    @table_top = table_top
  end

  def process_commands(commands_arr)
    # process given commands array
    # should outputs REPORT command to stdout
  end
end